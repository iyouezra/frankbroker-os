/**
 * Frank is currently a demo/test product. Authentication is intentionally
 * deferred, so every deployment uses the fictional role, tenant, and investor
 * personas. Do not connect this build to live or regulated data.
 */
export function isInsecureDemoMode() {
  return true;
}
